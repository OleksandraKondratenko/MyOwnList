+adding a value to the end
+adding a value to the beginning
+adding value by index
+remove one element from the end
+deleting one element from the beginning
+deleting one element at the index
--removing N elements from the end //M
--removing N elements from the beginning //M
--deleting by index N elements  //M
+return length
+index access
--first index by value //?
+change by index
+reverse (123 -> 321)
+find the value of the maximum element
+find the value of the minimum element
+search index of maximum element
+search index of the minimum element
--Sort Ascending //O
--descending sort //O
--delete by value first (? return index)//M
--delete by value all (? return count) //m
3 constructors (empty, one element based, array based) //?
--adding a list (your homemade) to the end //O
--adding a list to the beginning //O
--adding a list by index //O
